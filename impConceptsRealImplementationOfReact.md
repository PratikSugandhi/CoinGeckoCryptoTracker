# React imp concepts to know for implementation ->
- Set up vite react project,set up configuration for tailwind,esLint and daisy ui.
- Api call using xmlHttpRequest, fetch and axios.
- While using axios if there is any common part of url so make a helper project and inside it constant.js and then create axios instance.Axios instance should be create when there is common or base url or common header part.
- Use of Context API and the whole procedure is in main readme.md of notes.
- Uses of Zustand and the whole procedure is in main readme.md of notes.
- For zustand make store and wherever you want to use so call that store by importing and then by same imported name call it after adding pair of parenthesis.And it gives all the state variables and actions.Destructure all or whatever you want to destructure.